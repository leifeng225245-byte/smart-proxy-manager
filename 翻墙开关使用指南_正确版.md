# 翻墙开关使用指南

## 🎯 最简单的使用方法

### 🔥 每天就这几个命令
```bash
# 开翻墙
./mihomo-manager.sh on

# 关翻墙
./mihomo-manager.sh off

# 看状态
./mihomo-manager.sh status
```

## 📋 完整命令列表

### 🚀 基本操作
```bash
./mihomo-manager.sh on      # 开启翻墙
./mihomo-manager.sh off     # 关闭翻墙
./mihomo-manager.sh status  # 查看状态
./mihomo-manager.sh help    # 显示帮助
```

### 🔧 高级操作
```bash
./mihomo-manager.sh backup   # 手动备份
./mihomo-manager.sh restore  # 恢复配置
./mihomo-manager.sh test     # 测试网络
```

## 🎯 状态解读

### ✅ 开启翻墙状态
```
当前状态:
mihomo服务: 运行中
环境变量: 代理模式
  HTTP_PROXY=http://127.0.0.1:7890
  HTTPS_PROXY=http://127.0.0.1:7890
GNOME代理: 手动模式
7890端口: 监听中
保存状态: enabled
```

### ✅ 关闭翻墙状态
```
当前状态:
mihomo服务: 已停止
环境变量: 直连模式
GNOME代理: 无代理模式
7890端口: 未监听
保存状态: disabled
```

## 🔍 使用场景

### 🌐 场景1：需要访问国外网站
```bash
./mihomo-manager.sh on
# 然后可以访问 Google、YouTube、Twitter 等
```

### 🇨🇳 场景2：需要访问国内网站
```bash
./mihomo-manager.sh off
# 然后可以访问百度、微博、淘宝等
```

### 🔄 场景3：频繁切换
```bash
# 开启翻墙
./mihomo-manager.sh on

# 查看状态
./mihomo-manager.sh status

# 关闭翻墙
./mihomo-manager.sh off
```

## ⚠️ 注意事项

### 🎯 重要提醒
1. **一条命令搞定** - 不需要手动设置任何东西
2. **自动管理环境变量** - 脚本会自动处理
3. **自动管理GNOME代理** - 浏览器会自动生效
4. **状态自动保存** - 重启后会记住上次状态

### 🔧 故障排除

#### 问题1：翻墙开启后浏览器无法访问国外网站
```bash
# 检查状态
./mihomo-manager.sh status

# 确认GNOME代理是否为"手动模式"
gsettings get org.gnome.system.proxy mode

# 如果不是手动模式，重新开启
./mihomo-manager.sh off
./mihomo-manager.sh on
```

#### 问题2：翻墙关闭后浏览器无法访问国内网站
```bash
# 检查状态
./mihomo-manager.sh status

# 确认GNOME代理是否为"无代理模式"
gsettings get org.gnome.system.proxy mode

# 如果不是无代理模式，重新关闭
./mihomo-manager.sh on
./mihomo-manager.sh off
```

#### 问题3：脚本执行失败
```bash
# 检查脚本权限
ls -la ~/mihomo-manager.sh

# 如果没有执行权限，添加权限
chmod +x ~/mihomo-manager.sh

# 检查mihomo服务状态
systemctl status mihomo
```

## 🎯 新功能说明

### 🆕 v1.1 新增功能
1. **自动管理GNOME代理** - 浏览器自动生效
2. **自动设置环境变量** - 当前会话立即生效
3. **完整状态显示** - 显示所有相关信息
4. **智能状态记忆** - 重启后自动恢复状态

### 🔄 与旧版本的区别
| 功能 | 旧版本 | 新版本 |
|------|--------|--------|
| 环境变量 | 需要手动设置 | 自动设置 |
| GNOME代理 | 需要手动设置 | 自动设置 |
| 状态显示 | 基本信息 | 完整信息 |
| 浏览器兼容 | 可能有问题 | 完全兼容 |

## 📱 浏览器测试

### 🌐 测试翻墙功能
开启翻墙后，尝试访问：
- https://www.google.com
- https://www.youtube.com
- https://twitter.com

### 🇨🇳 测试直连功能
关闭翻墙后，尝试访问：
- https://www.baidu.com
- https://www.taobao.com
- https://weibo.com

## 🎊 使用技巧

### ⚡ 快速切换
```bash
# 创建别名（可选）
alias proxy-on='./mihomo-manager.sh on'
alias proxy-off='./mihomo-manager.sh off'
alias proxy-status='./mihomo-manager.sh status'

# 使用别名
proxy-on    # 开启翻墙
proxy-off   # 关闭翻墙
proxy-status # 查看状态
```

### 📋 状态检查脚本
```bash
# 创建状态检查脚本
cat > check-proxy.sh << 'EOF'
#!/bin/bash
echo "=== 翻墙状态检查 ==="
./mihomo-manager.sh status
echo ""
echo "=== 网络测试 ==="
echo "测试百度："
curl -I --connect-timeout 3 http://www.baidu.com 2>/dev/null | head -1
echo "测试Google："
curl -I --connect-timeout 3 --proxy http://127.0.0.1:7890 https://www.google.com 2>/dev/null | head -1
EOF

chmod +x check-proxy.sh
./check-proxy.sh
```

## 🎯 总结

### ✅ 最佳实践
1. **使用脚本管理** - 不要手动修改配置
2. **经常检查状态** - 确认配置正确
3. **遇到问题先重启** - off → on
4. **定期备份配置** - 使用backup命令

### 🎉 享受使用
- 🚀 **一条命令** - 简单快速
- 🔄 **自动切换** - 无需手动操作
- 📱 **浏览器兼容** - 完美支持
- 🛡️ **稳定可靠** - 自动备份恢复

---

**版本: v1.1**  
**更新时间: 2026-03-15**  
**功能: 完整的智能翻墙管理**  

**🎊 现在你可以轻松管理翻墙了！**
